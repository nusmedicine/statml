{
  "name": "book-statml",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Interactive widgets for teaching statistics and machine learning.",
  "scripts": {
    "dev": "node scripts/serve.mjs 8000",
    "check": "node scripts/check.mjs",
    "build": "node scripts/check.mjs && node scripts/build.mjs",
    "preview": "npm run build && npx --yes serve _site"
  },
  "engines": {
    "node": ">=20"
  }
}
